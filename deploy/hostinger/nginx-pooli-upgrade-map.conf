# Install: /etc/nginx/conf.d/pooli-upgrade-map.conf
# Included automatically from nginx http{} via conf.d/*.conf.
# Fixes unconditional Connection "upgrade" breaking HTTP keepalive.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
